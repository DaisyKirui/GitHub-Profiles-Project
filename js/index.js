document.addEventListener("DOMContentLoaded", () => {
    let inputForm = document.getElementById("create-task-form");
    inputForm.addEventListener("submit", createNovelTask);
  });

  // create-task-form == flowerType
  // new-task-description ==enterFlowerType

  // create-task-form == flowerNumber
  //new-task-description == enterFlowerNumber
